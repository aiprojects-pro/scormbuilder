# Runtime deps de SCORM Builder en producción.
# Generadas a partir de libreria/pyproject.toml + dependencias opcionales que
# queremos en el contenedor.

# --- core motor (libreria/pyproject.toml) ---
python-docx>=1.1.0
reportlab>=4.0.0
jinja2>=3.1.0
click>=8.1.0
rich>=13.0.0
pyyaml>=6.0
Pillow>=10.0.0

# --- HTTP server local (app_local.py) ---
flask>=3.0.0
werkzeug>=3.0.0

# --- TTS (opcional pero útil en producción) ---
gtts>=2.5.0

# --- IA (opcional, solo si ANTHROPIC_API_KEY presente) ---
anthropic>=0.25.0

# --- OCR (opcional, desactivado por defecto en build_complete_course) ---
# Si quieres habilitar conversión imagen→tabla manual desde el editor,
# descomenta estas 3 y reconstruye la imagen.
# opencv-python-headless>=4.8.0
# pytesseract>=0.3.10
# numpy>=1.24.0

# --- Saneamiento SVG para output IA ---
defusedxml>=0.7.1

# --- Métricas Prometheus (opcional pero recomendado) ---
prometheus-client>=0.20.0
